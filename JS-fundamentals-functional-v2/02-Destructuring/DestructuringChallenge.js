//1. Create a object that looks like this:
const obj = { name: 'Rusty', room: 'kitchen', weapon: 'candlestick' };

const { room, weapon } = obj;
console.log('room:', room);

console.log('weapon:', weapon);
